import { ConfigProvider } from 'antd'

ConfigProvider.config({
  theme: {
    primaryColor: '#FCB71E',
    errorColor: '#ff4d4f',
    warningColor: '#faad14',
    successColor: '#52c41a',
    infoColor: '#1890ff',
  },
})

export default ConfigProvider
